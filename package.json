{
  "dependencies": {
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "inquirer": "^8.2.4",
    "mysql2": "^3.2.4",
    "sequelize": "^6.31.0"
  },
  "name": "object-relational-mapping-orm-challenge-e-commerce-back-end",
  "description": "USER:\r AS A manager at an internet retail company\r I WANT a back end for my e-commerce website that uses the latest technologies\r SO THAT my company can compete with other e-commerce companies\r ACCEPTANCE:\r GIVEN a functional Express.js API\r WHEN I add my database name, MySQL username, and MySQL password to an environment variable file\r THEN I am able to connect to a database using Sequelize\r WHEN I enter schema and seed commands\r THEN a development database is created and is seeded with test data\r WHEN I enter the command to invoke the application\r THEN my server is started and the Sequelize models are synced to the MySQL database\r WHEN I open API GET routes in Insomnia Core for categories, products, or tags\r THEN the data for each of these routes is displayed in a formatted JSON\r WHEN I test API POST, PUT, and DELETE routes in Insomnia Core\r THEN I am able to successfully create, update, and delete data in my database",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "sequelize-cli": "^6.6.0"
  }
}
